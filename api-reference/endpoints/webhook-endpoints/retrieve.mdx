---
title: Retrieve a webhook endpoint
openapi: "GET /v1/webhook-endpoints/{id}"
---

Retrieve a single webhook endpoint by id. The signing `secret` is **not** included — it's only returned once, on creation.
